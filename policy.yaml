## list in which conditions our pipeline should fail
sast:
  critical_count:
    operator: greather_than
    value: 2
  high_count:
    operator: greather_than
    value: 2

sca:
  critical_count:
    operator: greather_than
    value: 2
  high_count:
    operator: greather_than
    value: 1

secret:
  critical_count:
    operator: greather_than
    value: 1
  high_count:
    operator: greather_than
    value: 1

license:
  contains:
  - AGPL
  - GPL
  - LGPL
  - Apache-2.0
